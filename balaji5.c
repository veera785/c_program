#include <stdio.h>
int main()
{
    int n=5,power=2,result=1;
    while(power>0)
    {
        result=result*n;
        power--;
    }
    printf("the power value of n is %d",result);
    return 0;        














}
